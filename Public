<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>ABS QE Study Schedule</title>
  </head>
  <body>
    <div id="root"></div>
  </body>
</html>
```

**File 3:** `src/App.js` — create a folder called `src`, then move your `.jsx` file into it and rename it `App.js`

---

**Step 2: Your repo structure should look like this:**
```
abs-qe-schedule/
├── package.json
├── public/
│   └── index.html
└── src/
    └── App.js
